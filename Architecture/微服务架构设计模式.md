# 微服务架构设计模式

> 作者：Chris Richardson  
> 原书名：Microservices Patterns  
> 主题：服务拆分、进程间通信、数据一致性、查询、测试、部署、可观测性与架构演进

## 目录

- [一句话总结](#一句话总结)
- [核心原则](#核心原则)
- [一、架构与模式语言](#一架构与模式语言)
- [二、从单体地狱到微服务](#二从单体地狱到微服务)
- [三、服务拆分策略](#三服务拆分策略)
- [四、进程间通信](#四进程间通信)
- [五、使用 Saga 管理事务](#五使用-saga-管理事务)
- [六、业务逻辑与领域模型](#六业务逻辑与领域模型)
- [七、事件溯源](#七事件溯源)
- [八、查询模式](#八查询模式)
- [九、外部 API 模式](#九外部-api-模式)
- [十、测试微服务](#十测试微服务)
- [十一、部署与发布](#十一部署与发布)
- [十二、可观测性](#十二可观测性)
- [十三、从单体迁移到微服务](#十三从单体迁移到微服务)
- [模式索引](#模式索引)
- [实践清单](#实践清单)
- [待继续补充](#待继续补充)

## 一句话总结

微服务架构的重点不是把系统拆小，而是在服务边界、数据所有权、通信方式、一致性、部署和运维能力之间做清楚的工程取舍。

## 核心原则

- 架构设计的核心是决策，尤其是那些难以回退、影响质量属性的决策。
- 服务边界应该围绕业务能力，而不是围绕技术分层或数据库表。
- 每个服务应拥有自己的数据，跨服务共享数据库会破坏自治性。
- 分布式系统的复杂度不可消除，只能通过模式、边界和自动化能力管理。
- 微服务带来的组织收益必须大于它引入的运维、测试和一致性成本。

## 一、架构与模式语言

### 架构为什么重要

架构的重要性在于它影响了应用的非功能性需求，也称为质量属性或系统能力，例如：

- 可维护性。
- 可测试性。
- 可部署性。
- 可扩展性。
- 可用性。
- 安全性。
- 性能。

架构不是“技术蓝图”本身，而是持续塑造系统演进空间的一组关键决策。

### 扩展立方体

Martin Abbott 和 Michael Fisher 在《The Art of Scalability》中提出扩展立方体：

- X 轴扩展：水平复制，通过克隆实例来扩展。
- Y 轴扩展：功能性分解，通过拆分不同业务功能来扩展。
- Z 轴扩展：数据分区，通过客户 ID、区域等维度把相似数据分区。

三种扩展方式的适用场景：

- X 轴：在多个实例之间做负载均衡，是扩展单体应用吞吐量和可用性的常用方法。
- Z 轴：运行多个应用实例，每个实例只负责数据的一个子集，适合数据量和事务量持续增长的场景。
- Y 轴：通过功能分解解决开发协作和系统复杂性增长的问题，是微服务架构的核心方向。

### 没有银弹

Fred Brooks 在《人月神话》中提出“软件工程没有银弹”。微服务同样不是银弹，它解决一类问题，同时也会引入一类新问题。

选择微服务之前要问：

- 当前痛点是部署慢、协作慢、扩展难，还是代码结构混乱？
- 单体是否已经经过模块化、测试和边界治理？
- 团队是否具备自动化测试、持续交付、监控告警和故障处理能力？
- 拆分后的复杂度是否会超过收益？

### 技术选择中的偏见

Neal Ford 提醒过架构决策中常见的“非黑即白”倾向。微服务讨论也很容易掉进这个陷阱：把单体视为落后，把微服务视为先进。

更合理的判断方式是把技术选择放回上下文：

- Gartner 光环曲线提醒我们，热门技术会经历过度期待、失望和理性采用。
- Jonathan Haidt 在《The Righteous Mind》中讨论了人们如何为既有立场寻找理由；技术选型中也要警惕先站队、后论证。
- 架构讨论应围绕质量属性、约束和演进路径，而不是围绕标签。

### 模式与模式语言

Christopher Alexander 的《A Pattern Language》提出了模式语言的思想。

在软件架构中：

- 模式是针对特定上下文中发生的问题的可复用解决方案。
- 模式语言是一组互相关联的模式，用来解决特定领域内的一系列问题。
- 软件模式通过定义一组互相协作的软件元素来解决架构或设计问题。

常用的模式结构：

- 需求：Forces，必须面对的约束和张力。
- 结果上下文：Resulting Context，采用模式后的好处、代价和新问题。
- 相关模式：Related Patterns，和当前模式存在协作、替代或演进关系的模式。

相关模式常见关系：

- 前导：Predecessor，催生当前模式需求的模式。
- 后续：Successor，用来解决当前模式引入的新问题的模式。
- 替代：Alternative，提供另一种解决方案。
- 泛化：Generalization，针对一个问题的一般性解决方案。
- 特化：Specialization，针对特定场景的具体解决方案。

## 二、从单体地狱到微服务

### 单体应用的优势

单体不是天然错误。早期单体通常更适合快速开发：

- 开发、调试和部署简单。
- 事务和查询容易实现。
- 本地运行门槛低。
- 团队较小时沟通成本低。

### 单体地狱的信号

当系统和团队持续增长，单体可能逐步变成阻碍：

- 构建和部署越来越慢。
- 小改动也需要全量发布。
- 模块边界被频繁穿透。
- 数据库成为所有模块的共享耦合点。
- 新人理解成本高。
- 不同团队互相等待，交付节奏被最慢部分拖住。
- 局部扩展困难，只能整体扩容。

### 微服务的收益

- 独立开发。
- 独立部署。
- 独立扩展。
- 技术栈可局部演进。
- 故障影响范围更容易隔离。
- 团队可以围绕业务能力组织。

### 微服务的代价

- 分布式通信复杂。
- 数据一致性更难。
- 测试更复杂。
- 部署和运维门槛更高。
- 排查问题需要跨服务追踪。
- 服务数量增长后，治理成本会显著上升。

结论：微服务不是默认选择，而是当单体的组织和技术成本超过分布式系统成本时的选择。

## 三、服务拆分策略

### 按业务能力拆分

业务能力是组织为了创造价值而必须具备的能力。按业务能力拆分服务，能让系统结构贴近业务结构。

示例：

- 订单服务：管理订单生命周期。
- 支付服务：处理支付授权和退款。
- 库存服务：维护库存占用和释放。
- 配送服务：处理履约和发货。

### 按子域拆分

借助领域驱动设计可以识别子域和限界上下文：

- 核心子域：业务差异化和竞争力所在。
- 支撑子域：支持核心业务但不是差异化来源。
- 通用子域：可采购或复用的通用能力。

每个限界上下文内部拥有统一的领域模型，跨上下文通过明确接口协作。

### 拆分时的阻力

- 网络延迟。
- 跨服务事务。
- 查询跨多个服务。
- 共享数据模型。
- 团队边界不清。
- 发布和运维能力不足。

### 拆分启发

- 优先拆变化频繁、发布节奏不同的部分。
- 优先拆边界清晰、依赖少的能力。
- 不要为了“微服务数量”拆分。
- 当两个模块总是一起变更，它们可能不该拆开。
- 当一个模块的修改经常阻塞其他团队，它可能需要独立出来。

## 四、进程间通信

### 通信方式

服务之间常见通信方式：

- 同步请求/响应：HTTP、REST、gRPC。
- 异步消息：消息队列、发布订阅、事件流。

同步通信更直观，但容易形成调用链和级联故障。异步通信更适合解耦和削峰，但会引入最终一致性、消息顺序和重复消费问题。

### API 设计

- API 应表达业务语义，而不是泄露内部数据表结构。
- 版本管理要明确，避免一次修改破坏所有调用方。
- 对外契约比内部实现更稳定。
- 服务间接口要尽量小，避免“远程对象调用”式的细粒度 API。

### 消息通信

消息通信适合：

- 事件通知。
- 异步流程。
- 长事务协作。
- 流量削峰。
- 服务间解耦。

消息通信需要处理：

- 消息重复。
- 消息丢失。
- 消息乱序。
- 消费失败。
- 幂等性。
- 可靠发布。

### 防止故障扩散

- 设置超时。
- 限制重试次数。
- 使用指数退避。
- 使用断路器。
- 使用舱壁隔离。
- 为关键链路设计降级策略。

## 五、使用 Saga 管理事务

### 为什么需要 Saga

在单体应用中，一个本地 ACID 事务可以同时更新多个表。微服务中，每个服务拥有自己的数据库，跨服务业务流程需要用 Saga 管理最终一致性。

Saga 是一组本地事务的序列。每个本地事务完成后，触发下一个步骤；如果某一步失败，执行补偿事务撤销之前的业务影响。

### 协同方式

#### 编排式 Saga

由一个协调器显式调用各服务。

优点：

- 流程清楚。
- 状态集中。
- 容易监控和排错。

缺点：

- 协调器可能变胖。
- 参与者容易依赖协调器。

#### 编舞式 Saga

各服务通过事件协作，没有中心协调器。

优点：

- 服务更自治。
- 更符合事件驱动风格。

缺点：

- 流程分散。
- 难以理解全局状态。
- 循环依赖风险更高。

### Saga 设计要点

- 明确每一步的本地事务。
- 明确失败后的补偿动作。
- 补偿不是数据库回滚，而是业务上的反向操作。
- 所有参与者都要支持幂等。
- 记录 Saga 状态，便于恢复和排查。
- 识别并处理并发更新带来的异常场景。

## 六、业务逻辑与领域模型

### 事务脚本与领域模型

简单业务可以用事务脚本，复杂业务更适合领域模型。

- 事务脚本：流程直观，适合简单 CRUD 和规则少的场景。
- 领域模型：把行为放进领域对象，适合规则复杂、状态转换丰富的场景。

### 聚合

聚合是一组领域对象的边界，外部只能通过聚合根访问内部对象。

聚合的价值：

- 定义一致性边界。
- 限制对象间的随意引用。
- 控制事务范围。
- 让业务规则有明确归属。

聚合设计建议：

- 聚合尽量小。
- 聚合之间通过 ID 引用。
- 一个事务只修改一个聚合，跨聚合协作交给 Saga。
- 聚合方法表达业务意图，而不是暴露字段修改。

## 七、事件溯源

### 基本思想

事件溯源不是保存当前状态，而是保存导致状态变化的一系列事件。当前状态可以通过重放事件得到。

适用场景：

- 需要完整审计历史。
- 业务状态变化过程很重要。
- 需要发布可靠领域事件。
- 需要构建多种读模型。

### 优点

- 天然保留审计日志。
- 事件可以驱动其他服务。
- 可以重建状态和派生视图。
- 更容易理解业务发生过什么。

### 代价

- 查询当前状态需要额外模型或快照。
- 事件模型一旦发布，演进要谨慎。
- 调试和排错方式不同于传统 CRUD。
- 团队需要理解事件版本、重放和幂等处理。

### 与事务性发件箱

如果不使用事件溯源，也常需要事务性发件箱模式：业务数据和待发布消息在同一个本地事务中写入，再由后台进程可靠发布消息。

## 八、查询模式

### API 组合

API 组合由查询方调用多个服务，再组装结果。

适合：

- 查询逻辑简单。
- 数据量不大。
- 可接受多个远程调用。

风险：

- 调用链变长。
- 延迟变高。
- 组合方容易知道太多服务细节。

### CQRS

命令查询职责分离：写模型处理命令和状态变更，读模型面向查询单独构建。

适合：

- 查询跨多个服务。
- 查询性能要求高。
- 读写模型差异明显。
- 需要多种查询视图。

代价：

- 读模型存在延迟。
- 需要处理同步失败。
- 需要更多存储和维护逻辑。

## 九、外部 API 模式

### API Gateway

API Gateway 为客户端提供统一入口，负责路由、认证、协议转换、聚合和限流等横切能力。

适合：

- 客户端不应该知道内部服务拓扑。
- 需要统一认证、限流和监控。
- 需要为不同客户端聚合数据。

风险：

- 可能演变成新的单体。
- 不应承载过多业务逻辑。
- 需要高可用设计。

### BFF

Backend for Frontend 为不同客户端提供不同后端入口。

适合：

- Web、移动端、第三方开放平台需求差异大。
- 不同客户端的展示和数据聚合逻辑明显不同。

## 十、测试微服务

### 测试金字塔

微服务测试应尽量把反馈前移：

- 单元测试：验证函数、类、聚合等局部逻辑。
- 组件测试：在服务边界内验证服务行为。
- 契约测试：验证服务提供方和消费方是否遵守 API 契约。
- 集成测试：验证服务与数据库、消息队列等基础设施的协作。
- 端到端测试：验证关键业务链路，但数量要控制。

### 消费者驱动契约

消费者驱动契约测试用于防止提供方破坏调用方依赖。

要点：

- 契约来自消费者真实需求。
- 提供方在发布前验证契约。
- 契约应纳入持续集成。

### 测试难点

- 服务依赖多，本地环境复杂。
- 异步消息难以断言。
- 分布式事务和最终一致性需要等待和重试。
- 端到端测试脆弱且成本高。

## 十一、部署与发布

### 部署模式

- 每台主机多个服务实例：资源利用率高，但隔离性差。
- 每台虚拟机一个服务实例：隔离性更好，但资源开销大。
- 每个容器一个服务实例：常见选择，便于标准化部署。
- Serverless：按事件运行，降低运维负担，但受平台约束。

### 发布策略

- 滚动发布。
- 蓝绿发布。
- 金丝雀发布。
- 灰度发布。
- 特性开关。

### 配置

- 配置应外部化，不要打包进镜像。
- 密钥要使用专门的安全机制管理。
- 配置变更要可追踪、可回滚。

## 十二、可观测性

### 三大支柱

- 日志：记录离散事件，帮助定位发生了什么。
- 指标：观察系统趋势和健康状态。
- 链路追踪：还原一次请求经过的服务路径。

### 微服务需要的运维能力

- 健康检查。
- 日志聚合。
- 分布式追踪。
- 指标和告警。
- 审计日志。
- 异常聚合。
- 服务发现。
- 自动化部署。

### 关键指标

- 请求量。
- 错误率。
- 延迟。
- 饱和度。
- 消息堆积。
- Saga 失败率。
- 数据同步延迟。

## 十三、从单体迁移到微服务

### 绞杀者模式

绞杀者模式通过在单体外围逐步构建新服务，把部分请求切到新实现，最终替换旧功能。

步骤：

1. 识别单体中的候选能力。
2. 在单体外建立新服务。
3. 通过网关或路由把部分请求转向新服务。
4. 保持数据同步或逐步迁移数据所有权。
5. 删除单体中的旧实现。

### 防腐层

新服务不应直接继承单体的模型和混乱边界。防腐层用于隔离旧系统概念，把旧模型翻译成新服务自己的领域语言。

### 迁移建议

- 先模块化单体，再拆分服务。
- 从边界清晰、风险可控的能力开始。
- 不要一开始就拆数据库最复杂的核心链路。
- 每拆出一个服务，都要补齐部署、监控和测试能力。
- 迁移过程要持续创造业务价值，而不是长期停留在架构工程里。

## 模式索引

| 模式 | 解决的问题 | 主要代价 |
| --- | --- | --- |
| 按业务能力拆分 | 服务边界如何划分 | 需要深入理解业务 |
| 按子域拆分 | 领域模型和团队边界如何对齐 | 需要 DDD 建模能力 |
| API Gateway | 客户端如何访问多个服务 | 网关可能变成瓶颈或新单体 |
| BFF | 不同客户端如何获得合适 API | 后端入口数量增加 |
| Saga | 跨服务事务如何保持一致 | 最终一致性和补偿逻辑复杂 |
| 编排式 Saga | 如何集中管理长流程 | 协调器可能过重 |
| 编舞式 Saga | 如何用事件驱动协作 | 全局流程不直观 |
| API 组合 | 如何跨服务查询数据 | 延迟和耦合增加 |
| CQRS | 如何支持复杂查询 | 读模型同步复杂 |
| 事件溯源 | 如何记录完整状态变化历史 | 查询和事件演进复杂 |
| 事务性发件箱 | 如何可靠发布事件 | 需要额外转发机制 |
| 断路器 | 如何防止故障扩散 | 需要合理阈值和监控 |
| 消费者驱动契约 | 如何避免 API 破坏调用方 | 契约维护成本 |
| 绞杀者模式 | 如何逐步替换单体 | 迁移期双系统复杂 |
| 防腐层 | 如何隔离旧模型污染 | 增加转换层 |

## 实践清单

### 采用微服务前

- 是否已经确认单体的主要瓶颈？
- 是否能用模块化单体先解决问题？
- 团队是否具备持续交付能力？
- 是否有自动化测试基础？
- 是否有日志、指标、追踪和告警能力？
- 是否能接受最终一致性？

### 设计服务时

- 服务是否围绕业务能力？
- 数据所有权是否清晰？
- 是否避免共享数据库？
- API 是否表达业务语义？
- 跨服务调用是否有超时、重试、熔断和降级？
- 异步消息是否支持幂等和可靠发布？
- 关键业务流程是否有 Saga 状态记录？

### 运维服务时

- 每个服务是否有健康检查？
- 是否能追踪一次请求跨越哪些服务？
- 是否能定位消息堆积和消费失败？
- 是否有发布回滚策略？
- 是否有容量和错误率告警？
- 是否能在本地或测试环境复现关键依赖？

## 待继续补充

- 补充书中每个模式的页码和原文摘录。
- 为 Saga、CQRS、事件溯源补充具体业务案例。
- 结合当前项目画出候选服务边界。
- 补充“什么时候不应该使用微服务”的反例。
- 整理一份微服务落地前的团队能力成熟度清单。
